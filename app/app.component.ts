import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'concert-app',
    template: `
        <h1>Angular2: Cloudant Integration</h1>
        <router-outlet><router-outlet>
    `
})
export class AppComponent { }
